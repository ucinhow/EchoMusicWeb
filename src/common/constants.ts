import { Source, PlaySource, DataType } from "./typings";
export const SOURCE = Object.values(Source);
export const PLAYSOURCE = Object.values(PlaySource);

export const NONESRC = "NONE_SOURCE";

export const REQUEST_ERROR_MSG = "网络请求错误";

export const SEARCHTYPE = [DataType.song, DataType.album, DataType.songlist];
